#include "holberton.h"
/**
 * print_binary - prints an int in binary
 * @n: unsigned and long
 */
void print_binary(unsigned long int n)
{
	unsigned int digit;

	while (n > 0)
	{
		for (digit = 1; 
}
